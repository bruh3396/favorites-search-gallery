export enum PerformanceProfile {
  NORMAL = 0,
  MEDIUM = 3,
  LOW = 1,
  POTATO = 2
}

export enum ClickCodes {
  LEFT = 0,
  MIDDLE = 1,
  RIGHT = 2
}

export enum DiscreteRating {
  EXPLICIT = 4,
  QUESTIONABLE = 2,
  SAFE = 1,
}
