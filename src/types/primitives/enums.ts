export enum PerformanceProfile {
  LOW = 2,
  MEDIUM = 1,
  HIGH = 0
}

export enum ClickCodes {
  LEFT = 0,
  MIDDLE = 1,
  RIGHT = 2
}

export enum DiscreteRating {
  EXPLICIT = 4,
  QUESTIONABLE = 2,
  SAFE = 1,
}
